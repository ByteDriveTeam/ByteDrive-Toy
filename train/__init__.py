"""训练 / 优化 / 评估循环包标识：只读消费 config。

模块: train/__init__.py
依赖: —
读取配置: —（各实现文件通过入参接收 cfg）
对外接口: —（仅作为包标识，实际接口见各子模块）
说明: 训练循环、优化器构造、多任务损失各自成子模块文件夹 train/<模块>/（__init__ 重导出公开 API）；
      CLI 入口 run.py 留在包根，其入口校验在 train/checks/。按需从 train.<模块> 导入。
"""
