"""网络结构定义包标识：只读消费 config，不含可调参数默认值。

模块: model/__init__.py
依赖: —
读取配置: —（本包各实现文件通过入参接收 cfg，自身不在此聚合）
对外接口: —（仅作为包标识，实际接口见各子模块）
说明: 迁移自另一代码库时 Index 已登记本文件但实体缺失，此处补齐为空包标识；
      网络结构定义均在同目录各实现文件中，按需从 model.<模块> 导入。
"""
